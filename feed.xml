<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>胤源 Blog</title>
  <link href="https://691407323.github.io/feed.xml" rel="self"/>
  <link href="https://691407323.github.io/"/>
  <updated>2026-07-04T03:50:24Z</updated>
  <author><name>胤源 Blog</name></author>
  <id>https://691407323.github.io/</id>
  
  <entry>
    <title>在博客中插入图片示例</title>
    <summary>演示在本项目中如何添加/引用图片，含 cover 封面、WebP 自动优化、缩略图与点击放大。</summary>
    <link href="https://691407323.github.io/posts/image-usage-demo.html"/>
    <id>https://691407323.github.io/posts/image-usage-demo.html</id>
    <updated>2026-07-04T00:00:00Z</updated>
    <content type="html"><blockquote>
<p>本篇是本项目的「图片使用手册」，把图片从放进仓库到前端展示的完整链路走一遍。
涉及模块：M-4 front matter 的 <code>cover</code>/<code>tags</code> 字段、M-13 图片自动 WebP + 缩略图、M-14 <code>og:image</code>。</p>
</blockquote>
<h2 id="_1">一、图片放在哪里</h2>
<p>本项目的图片统一放在 <code>static/images/</code> 目录下。构建时 <code>build.py</code> 会把整个 <code>static/</code> 原样复制到 <code>_site/static/</code>，所以图片最终可访问路径为：</p>
<div class="highlight"><pre><span></span><code>/static/images/&lt;文件名&gt;
</code></pre></div>

<p>对应的项目结构：</p>
<div class="highlight"><pre><span></span><code>Blog/
├── content/posts/        # Markdown 文章
├── static/images/        # ← 图片放这里
│   └── sample-desktop.png
├── build.py              # 构建脚本（含 M-13 图片处理）
└── _site/                # 构建产物（部署目录）
    └── static/images/
        ├── sample-desktop.png           # 原图（原样复制）
        ├── sample-desktop.webp           # ← 构建自动生成，主图
        └── sample-desktop_thumb.webp    # ← 构建自动生成，缩略图
</code></pre></div>

<blockquote>
<p>⚠️ <strong>关键决策</strong>：Markdown 里的图片路径<strong>不会被自动改写</strong>（参见 <code>PLAN.md</code> M-13）。想用 <code>.webp</code> 就手写 <code>.webp</code>，想用 <code>.png</code> 就保留 <code>.png</code>。这避免了"自动替换但链接算不准"的坑。</p>
</blockquote>
<h2 id="_2">二、正文插入图片（直接用原图）</h2>
<p>标准 Markdown 语法，绝对路径引用：</p>
<div class="highlight"><pre><span></span><code>![<span class="nt">示例图片</span>](<span class="na">/static/images/sample-desktop.png</span>)
</code></pre></div>

<p>效果如下（这是原图 <code>sample-desktop.png</code>，457KB）：</p>
<p><img alt="示例图片" src="/static/images/sample-desktop.png" /></p>
<h2 id="webp">三、用自动生成的 WebP（推荐）</h2>
<p>构建时 <code>build.py</code> 的 <code>process_images()</code> 会为每张 PNG/JPG 自动生成：</p>
<p>| 产物 | 宽度 | 体积 | 用途 |
|------|------|------|------|
| <code>sample-desktop.png</code> | 775px | 457KB | 原图，原样复制 |
| <code>sample-desktop.webp</code> | 775px | <strong>55KB</strong> | 主图，质量 82，体积 <strong>↓88%</strong> |
| <code>sample-desktop_thumb.webp</code> | 480px | <strong>20KB</strong> | 缩略图，列表/卡片场景 |</p>
<p>同样一张图，引用 <code>.webp</code> 即可享受 8 倍体积优势：</p>
<div class="highlight"><pre><span></span><code>![<span class="nt">示例图片 WebP</span>](<span class="na">/static/images/sample-desktop.webp</span>)
</code></pre></div>

<p><img alt="示例图片 WebP" src="/static/images/sample-desktop.webp" /></p>
<p>肉眼几乎看不出差别，但读者流量省了近 400KB。</p>
<h2 id="_3">四、缩略图 + 点击放大</h2>
<p>缩略图 <code>_thumb.webp</code> 适合正文预览，链接指向大图，用户点开再加载完整图：</p>
<div class="highlight"><pre><span></span><code>[<span class="nt">![缩略图</span>](<span class="na">/static/images/sample-desktop_thumb.webp</span>)](/static/images/sample-desktop.webp)
</code></pre></div>

<p>效果（点缩略图跳转到主图）：</p>
<p><a href="/static/images/sample-desktop.webp"><img alt="缩略图" src="/static/images/sample-desktop_thumb.webp" /></a></p>
<h2 id="cover-ogimage">五、封面图（cover 字段 → og:image）</h2>
<p>在 front matter 用 <code>cover</code> 指定，构建时会自动拼成 <code>og:image</code> 绝对 URL，用于社交平台分享卡片：</p>
<div class="highlight"><pre><span></span><code><span class="nn">---</span>
<span class="nt">title</span><span class="p">:</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">在博客中插入图片示例</span>
<span class="nt">cover</span><span class="p">:</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">static/images/sample-desktop.png</span>
<span class="nn">---</span>
</code></pre></div>

<p>注意 front matter 里<strong>不加前导斜杠</strong>（写 <code>static/...</code>）。构建时 <code>build.py</code> 会拼成：</p>
<div class="highlight"><pre><span></span><code>https://691407323.github.io/static/images/sample-desktop.png
</code></pre></div>

<blockquote>
<p>👉 在本页面右键「查看网页源代码」，搜 <code>og:image</code> 就能看到这行已自动渲染。
M-14 的设计是<strong>条件输出</strong>：没有 <code>cover</code> 的文章不会输出空的 <code>og:image</code> 标签。</p>
</blockquote>
<h2 id="_4">六、完整工作流</h2>
<ol>
<li>把图片放进 <code>static/images/</code>（建议英文/数字命名）</li>
<li>运行 <code>python3 build.py</code></li>
<li><code>process_images()</code> 自动为每张 PNG/JPG 生成 <code>.webp</code> + <code>_thumb.webp</code></li>
<li>在 Markdown 正文里引用产物路径（<code>.webp</code> 最省流量）</li>
<li>需要社交分享卡片时，在 front matter 加 <code>cover:</code></li>
<li>提交 <code>_site/</code> 到 Pages 仓库即可线上生效</li>
</ol>
<h2 id="_5">七、关键提醒与小坑</h2>
<ul>
<li><strong>路径不自动改写</strong>：写文章时直接用最终产物路径，不要等构建去替换</li>
<li><strong>增量跳过</strong>：源图没改时下次构建会跳过（看日志 <code>0 张生成，1 张跳过</code>），不会重复转</li>
<li><strong>Pillow 软依赖</strong>：没安装 Pillow 时构建照常通过，只是跳过图片处理并打印提示，不报错</li>
<li><strong>中文文件名</strong>：理论上可用，但会触发 URL 编码，强烈建议英文/数字命名</li>
<li><strong>WebP 兼容率 95%+</strong>：本项目决策是只转 WebP 不保留原格式兜底，读者群体浏览器足够新</li>
<li><strong>移动端自适应</strong>：CSS 已设置 <code>.post-body img { max-width:100% }</code>（见 <code>static/css/style.css:48</code>），无需手动控制宽度</li>
<li><strong><code>cover</code> 是分享卡片缩略图</strong>，不是文章顶部的 banner；要做文章头图需要自己在正文最上方插一张图</li>
</ul></content>
  </entry>
  
  <entry>
    <title>Skynet-cmake 跨平台使用</title>
    <summary>基于 hanxi/skynet-cmake 的跨平台构建方案，skynet 以 submodule 方式接入，并引入 TSTL 替代传统 Lua 脚本。</summary>
    <link href="https://691407323.github.io/posts/skynet-cmake-usage.html"/>
    <id>https://691407323.github.io/posts/skynet-cmake-usage.html</id>
    <updated>2026-06-03T00:00:00Z</updated>
    <content type="html"><h2 id="_1">简介</h2>
<p>本篇基于 <a href="https://github.com/hanxi/skynet-cmake">hanxi/skynet-cmake</a> 项目。skynet 以 submodule 的方式链接，方便升级且保持不可改原则。开发环境为 Visual Studio 2022，需安装 CMake 与 Clang 组件。在此基础上做了若干跨平台调整，同时引入 TSTL（TypeScriptToLua）来替代传统 Lua 脚本编写方式。</p>
<p>项目地址：<a href="https://github.com/691407323/skynet-cmake.git">skynet-cmake</a></p>
<h2 id="_2">项目层级</h2>
<p>项目目录结构调整如下：</p>
<pre id="folder-tree">
core/
├── skynet/
├── posix/
└── pthread-win32/
cservice-src/
luaclib-src/
└── <a href="#1-lua-winhelp-windows" style="color:#4fc3f7;">[→ lua-winhelp]</a>
ts/
├── src/
├── tools/
└── interface/
build/
buildWin/
CMakeLists.txt
</pre>

<h2 id="skynet-cmake-win">Skynet-cmake-win 修改记录</h2>
<h3 id="1-unistdh-log">1. 修改 <code>unistd.h</code> 启动 log 调用</h3>
<p>时间转换函数在 Windows 和 Linux 上表现不一致：</p>
<ul>
<li><strong>Windows</strong>：<code>_localtime64(struct tm, time_t)</code></li>
<li><strong>Linux</strong>：<code>localtime_r(time_t, struct tm)</code></li>
</ul>
<p>调用方式需要区分平台，避免参数顺序导致的编译或运行时错误。</p>
<h3 id="2-windows-socket">2. 修改 Windows 下 socket 断开问题</h3>
<p>Windows 下 <code>read</code> 函数在 socket 非阻塞模式下遇到 <code>WSAEWOULDBLOCK</code> 时会被当作异常断开。修复方案：</p>
<div class="highlight"><pre><span></span><code><span class="kt">int</span><span class="w"> </span><span class="n">wsaerr</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">WSAGetLastError</span><span class="p">();</span>
<span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="n">wsaerr</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="n">WSAEWOULDBLOCK</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="n">errno</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">EWOULDBLOCK</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div>

<p>将 <code>WSAEWOULDBLOCK</code> 映射为 <code>EWOULDBLOCK</code>，使上层逻辑无需区分平台即可正确处理非阻塞读未就绪的情况。</p>
<h2 id="_3">附录</h2>
<h3 id="1-lua-winhelp-windows">1. lua-winhelp — Windows 控制台输入模块 <a href="#folder-tree" style="color:#4fc3f7;">[↩]</a></h3>
<p>在 Windows 平台下，skynet 的控制台输入处理与 Linux 存在差异。<code>lua-winhelp.c</code> 提供了一套 Windows 原生的 stdin 解决方案，通过独立线程异步读取控制台输入，避免阻塞 skynet 主线程。</p>
<h4 id="_4">核心设计</h4>
<ul>
<li>使用 <code>atomic_bool</code> 标记输入是否就绪，实现线程间安全通信</li>
<li>后台线程通过 <code>fgets</code> 阻塞读取 stdin，就绪后通知主线程</li>
<li>提供三个 Lua 接口：初始化线程、获取输入结果、关闭线程</li>
</ul>
<div class="highlight"><pre><span></span><code><span class="k">static</span><span class="w"> </span><span class="n">DWORD</span><span class="w"> </span><span class="n">WINAPI</span><span class="w"> </span><span class="n">readInput</span><span class="p">(</span><span class="n">LPVOID</span><span class="w"> </span><span class="n">lpParam</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="k">while</span><span class="p">(</span><span class="o">!</span><span class="n">input_end</span><span class="p">){</span>
<span class="w">        </span><span class="n">fgets</span><span class="p">(</span><span class="n">input</span><span class="p">,</span><span class="w"> </span><span class="k">sizeof</span><span class="p">(</span><span class="n">input</span><span class="p">),</span><span class="w"> </span><span class="n">stdin</span><span class="p">);</span><span class="w">  </span><span class="c1">// 阻塞读取输入</span>
<span class="w">        </span><span class="n">input_ready</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">TRUE</span><span class="p">;</span><span class="w">                  </span><span class="c1">// 标记输入已准备好</span>
<span class="w">    </span><span class="p">}</span>
<span class="w">    </span><span class="k">return</span><span class="w"> </span><span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div>

<h4 id="lua">Lua 接口</h4>
<table>
<thead>
<tr><th>函数</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td><code>initWinThread()</code></td><td>启动后台输入线程</td></tr>
<tr><td><code>getWinThreadResultFlag()</code></td><td>获取用户输入字符串，无输入返回 <code>nil</code></td></tr>
<tr><td><code>closeWinThread()</code></td><td>关闭线程并等待退出</td></tr>
</tbody>
</table>

<h4 id="_5">完整源码</h4>
<div class="highlight"><pre><span></span><code><span class="c1">//</span>
<span class="c1">// Created by Vermouth on 2024/1/24.</span>
<span class="c1">//</span>

<span class="cp">#include</span><span class="w"> </span><span class="cpf">&quot;lua.h&quot;</span>
<span class="cp">#include</span><span class="w"> </span><span class="cpf">&quot;lauxlib.h&quot;</span>
<span class="cp">#include</span><span class="w"> </span><span class="cpf">&quot;lualib.h&quot;</span>
<span class="cp">#include</span><span class="w"> </span><span class="cpf">&lt;stdio.h&gt;</span>
<span class="cp">#include</span><span class="w"> </span><span class="cpf">&lt;stdlib.h&gt;</span>
<span class="cp">#include</span><span class="w"> </span><span class="cpf">&lt;windows.h&gt;</span>
<span class="cp">#include</span><span class="w"> </span><span class="cpf">&lt;stdatomic.h&gt;</span>

<span class="k">static</span><span class="w"> </span><span class="kt">atomic_bool</span><span class="w"> </span><span class="n">input_ready</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">ATOMIC_VAR_INIT</span><span class="p">(</span><span class="n">FALSE</span><span class="p">);</span><span class="w">  </span><span class="c1">// 原子变量，表示输入是否准备好</span>
<span class="k">static</span><span class="w"> </span><span class="n">BOOL</span><span class="w"> </span><span class="n">input_end</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">FALSE</span><span class="p">;</span>
<span class="k">static</span><span class="w"> </span><span class="kt">char</span><span class="w"> </span><span class="n">input</span><span class="p">[</span><span class="mi">256</span><span class="p">];</span><span class="w">                                   </span><span class="c1">// 存储用户输入的全局变量</span>
<span class="k">static</span><span class="w"> </span><span class="n">HANDLE</span><span class="w"> </span><span class="n">input_thread</span><span class="p">;</span>

<span class="k">static</span><span class="w"> </span><span class="n">DWORD</span><span class="w"> </span><span class="n">WINAPI</span><span class="w"> </span><span class="n">readInput</span><span class="p">(</span><span class="n">LPVOID</span><span class="w"> </span><span class="n">lpParam</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="k">while</span><span class="p">(</span><span class="o">!</span><span class="n">input_end</span><span class="p">){</span>
<span class="w">        </span><span class="n">fgets</span><span class="p">(</span><span class="n">input</span><span class="p">,</span><span class="w"> </span><span class="k">sizeof</span><span class="p">(</span><span class="n">input</span><span class="p">),</span><span class="w"> </span><span class="n">stdin</span><span class="p">);</span><span class="w">  </span><span class="c1">// 阻塞读取输入</span>
<span class="w">        </span><span class="n">input_ready</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">TRUE</span><span class="p">;</span><span class="w">                  </span><span class="c1">// 标记输入已准备好</span>
<span class="w">    </span><span class="p">}</span>
<span class="w">    </span><span class="k">return</span><span class="w"> </span><span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span><span class="w"> </span><span class="kt">int</span><span class="w"> </span><span class="n">linitWinThread</span><span class="p">(</span><span class="n">lua_State</span><span class="w"> </span><span class="o">*</span><span class="n">L</span><span class="p">){</span>
<span class="w">    </span><span class="n">input_thread</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">CreateThread</span><span class="p">(</span>
<span class="w">            </span><span class="nb">NULL</span><span class="p">,</span><span class="w">          </span><span class="c1">// 默认安全属性</span>
<span class="w">            </span><span class="mi">0</span><span class="p">,</span><span class="w">             </span><span class="c1">// 默认堆栈大小</span>
<span class="w">            </span><span class="n">readInput</span><span class="p">,</span><span class="w">     </span><span class="c1">// 线程函数</span>
<span class="w">            </span><span class="nb">NULL</span><span class="p">,</span><span class="w">          </span><span class="c1">// 参数</span>
<span class="w">            </span><span class="mi">0</span><span class="p">,</span><span class="w">             </span><span class="c1">// 立即运行线程</span>
<span class="w">            </span><span class="nb">NULL</span><span class="w">           </span><span class="c1">// 不需要线程ID</span>
<span class="w">    </span><span class="p">);</span>
<span class="w">    </span><span class="n">input_ready</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">FALSE</span><span class="p">;</span>
<span class="w">    </span><span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="n">input_thread</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="nb">NULL</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">        </span><span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span><span class="w"> </span><span class="s">&quot;无法创建线程</span><span class="se">\n</span><span class="s">&quot;</span><span class="p">);</span>
<span class="w">        </span><span class="k">return</span><span class="w"> </span><span class="mi">1</span><span class="p">;</span>
<span class="w">    </span><span class="p">}</span>
<span class="w">    </span><span class="k">return</span><span class="w"> </span><span class="mi">1</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span><span class="w"> </span><span class="kt">int</span><span class="w"> </span><span class="n">lgetWinThreadResultFlag</span><span class="p">(</span><span class="n">lua_State</span><span class="w"> </span><span class="o">*</span><span class="n">L</span><span class="p">){</span>
<span class="w">    </span><span class="k">if</span><span class="p">(</span><span class="n">input_ready</span><span class="p">){</span>
<span class="w">        </span><span class="n">lua_pushstring</span><span class="p">(</span><span class="n">L</span><span class="p">,</span><span class="w"> </span><span class="n">input</span><span class="p">);</span>
<span class="w">    </span><span class="p">}</span><span class="k">else</span><span class="p">{</span>
<span class="w">        </span><span class="n">lua_pushnil</span><span class="p">(</span><span class="n">L</span><span class="p">);</span>
<span class="w">    </span><span class="p">}</span>
<span class="w">    </span><span class="n">input_ready</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">FALSE</span><span class="p">;</span>
<span class="w">    </span><span class="k">return</span><span class="w"> </span><span class="mi">1</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span><span class="w"> </span><span class="kt">int</span><span class="w"> </span><span class="n">lcloseWinThread</span><span class="p">(</span><span class="n">lua_State</span><span class="w"> </span><span class="o">*</span><span class="n">L</span><span class="p">){</span>
<span class="w">    </span><span class="n">input_end</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">TRUE</span><span class="p">;</span>
<span class="w">    </span><span class="n">WaitForSingleObject</span><span class="p">(</span><span class="n">input_thread</span><span class="p">,</span><span class="w"> </span><span class="n">INFINITE</span><span class="p">);</span>
<span class="w">    </span><span class="n">CloseHandle</span><span class="p">(</span><span class="n">input_thread</span><span class="p">);</span>
<span class="p">}</span>

<span class="k">static</span><span class="w"> </span><span class="k">const</span><span class="w"> </span><span class="n">luaL_Reg</span><span class="w"> </span><span class="n">l</span><span class="p">[]</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">{</span>
<span class="w">        </span><span class="p">{</span><span class="s">&quot;initWinThread&quot;</span><span class="p">,</span><span class="n">linitWinThread</span><span class="p">},</span>
<span class="w">        </span><span class="p">{</span><span class="s">&quot;getWinThreadResultFlag&quot;</span><span class="p">,</span><span class="n">lgetWinThreadResultFlag</span><span class="p">},</span>
<span class="w">        </span><span class="p">{</span><span class="s">&quot;closeWinThread&quot;</span><span class="p">,</span><span class="n">lcloseWinThread</span><span class="p">},</span>
<span class="w">        </span><span class="p">{</span><span class="nb">NULL</span><span class="p">,</span><span class="w"> </span><span class="nb">NULL</span><span class="p">},</span>
<span class="p">};</span>

<span class="kt">int</span><span class="w"> </span><span class="nf">luaopen_winhelp</span><span class="p">(</span><span class="n">lua_State</span><span class="w"> </span><span class="o">*</span><span class="n">L</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="n">luaL_newlib</span><span class="p">(</span><span class="n">L</span><span class="p">,</span><span class="w"> </span><span class="n">l</span><span class="p">);</span>
<span class="w">    </span><span class="k">return</span><span class="w"> </span><span class="mi">1</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></content>
  </entry>
  
  <entry>
    <title>Markdown 语法指南</title>
    <summary>Markdown 语法指南</summary>
    <link href="https://691407323.github.io/posts/markdown-guide.html"/>
    <id>https://691407323.github.io/posts/markdown-guide.html</id>
    <updated>2026-05-29T00:00:00Z</updated>
    <content type="html"><p>这是一篇全面展示 Markdown 语法的示例文章，帮助你快速上手写作。</p>
<h2 id="_1">文本样式</h2>
<p><strong>粗体文字</strong>、<em>斜体文字</em>、~~删除线~~、<code>行内代码</code>。</p>
<p>也可以组合：<strong><em>粗体加斜体</em></strong>，<strong>粗体中的 <code>代码</code></strong>。</p>
<h2 id="_2">标题层级</h2>
<p>文章标题用 <code>##</code> 开始（<code>#</code> 保留给页面标题），正文内可以用三级、四级标题。</p>
<h3 id="_3">三级标题</h3>
<p>这是三级标题下的段落。</p>
<h4 id="_4">四级标题</h4>
<p>更细分的层级。</p>
<h2 id="_5">列表</h2>
<h3 id="_6">无序列表</h3>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>嵌套子项 A</li>
<li>嵌套子项 B<ul>
<li>更深层级</li>
</ul>
</li>
<li>第三项</li>
</ul>
<h3 id="_7">有序列表</h3>
<ol>
<li>第一步：安装依赖</li>
<li>第二步：配置环境</li>
<li>第三步：启动服务</li>
<li>检查端口占用</li>
<li>运行启动命令</li>
</ol>
<h3 id="_8">任务列表</h3>
<ul>
<li>[x] 已完成的任务</li>
<li>[x] 另一项已完成</li>
<li>[ ] 待办事项</li>
<li>[ ] 另一项待办</li>
</ul>
<h2 id="_9">引用</h2>
<blockquote>
<p>这是一段引用文字。可以包含<strong>粗体</strong>、<em>斜体</em>等内联样式。</p>
<p>多层引用：</p>
<blockquote>
<p>嵌套引用层级 2</p>
<p>同一层级第二段</p>
</blockquote>
<p>回到第一层。</p>
</blockquote>
<h2 id="_10">表格</h2>
<p>| 名称 | 版本 | 说明 |
|------|------|------|
| Python | 3.12 | 构建脚本语言 |
| Jinja2 | 3.x | 模板引擎 |
| Pygments | 2.x | 代码高亮 |</p>
<p>对齐方式：</p>
<p>| 左对齐 | 居中 | 右对齐 |
|:-------|:----:|-------:|
| 内容 A | 内容 B | 100 |
| 长内容 C | D | 999 |</p>
<h2 id="_11">链接与图片</h2>
<ul>
<li>外部链接：<a href="https://www.python.org/">Python 官网</a></li>
<li>自动链接：<a href="https://github.com">https://github.com</a></li>
<li>引用式链接：<a href="https://example.com" title="鼠标悬停提示">点击查看</a></li>
</ul>
<p>图片示例：<code>![替代文字](image-url.png)</code></p>
<h2 id="_12">分隔线</h2>
<p>这是分隔线上方的内容。</p>
<hr />
<p>分隔线下方的内容。</p>
<h2 id="_13">脚注</h2>
<p>这是一段带脚注的文字<sup id="fnref:1"><a class="footnote-ref" href="#fn:1">1</a></sup>。脚注会自动渲染到页面底部。</p>
<h2 id="html">HTML 内嵌</h2>
<p>Markdown 支持直接嵌入 HTML：</p>
<details>
<summary>点击展开详情</summary>

这里是被折叠的内容，可以包含 Markdown 格式。

- 列表项
- **粗体**

</details>

<h2 id="_14">总结</h2>
<p>以上涵盖了 Markdown 的主要语法特性。你可以参考这篇文章的源文件 <code>content/posts/markdown-guide.md</code> 来学习写法。</p>
<div class="footnote">
<hr />
<ol>
<li id="fn:1">
<p>这是脚注的内容，可以写很长的说明。&#160;<a class="footnote-backref" href="#fnref:1" title="Jump back to footnote 1 in the text">&#8617;</a></p>
</li>
</ol>
</div></content>
  </entry>
  
  <entry>
    <title>Docker 部署 Python 应用</title>
    <summary>Docker 部署 Python 应用</summary>
    <link href="https://691407323.github.io/posts/docker-python-deploy.html"/>
    <id>https://691407323.github.io/posts/docker-python-deploy.html</id>
    <updated>2026-05-28T00:00:00Z</updated>
    <content type="html"><p>本文演示如何使用 Docker 容器化部署一个 Python Web 应用，并展示多语言代码高亮效果。</p>
<h2 id="_1">项目结构</h2>
<div class="highlight"><pre><span></span><code>myapp/
├── app.py          # Flask 应用
├── requirements.txt
└── Dockerfile
</code></pre></div>

<h2 id="python">Python 代码示例</h2>
<div class="highlight"><pre><span></span><code><span class="kn">from</span><span class="w"> </span><span class="nn">flask</span><span class="w"> </span><span class="kn">import</span> <span class="n">Flask</span><span class="p">,</span> <span class="n">jsonify</span>

<span class="n">app</span> <span class="o">=</span> <span class="n">Flask</span><span class="p">(</span><span class="vm">__name__</span><span class="p">)</span>

<span class="nd">@app</span><span class="o">.</span><span class="n">route</span><span class="p">(</span><span class="s2">&quot;/api/health&quot;</span><span class="p">)</span>
<span class="k">def</span><span class="w"> </span><span class="nf">health</span><span class="p">():</span>
<span class="w">    </span><span class="sd">&quot;&quot;&quot;健康检查接口&quot;&quot;&quot;</span>
    <span class="k">return</span> <span class="n">jsonify</span><span class="p">({</span><span class="s2">&quot;status&quot;</span><span class="p">:</span> <span class="s2">&quot;ok&quot;</span><span class="p">,</span> <span class="s2">&quot;version&quot;</span><span class="p">:</span> <span class="s2">&quot;1.0.0&quot;</span><span class="p">})</span>

<span class="nd">@app</span><span class="o">.</span><span class="n">route</span><span class="p">(</span><span class="s2">&quot;/api/users&quot;</span><span class="p">)</span>
<span class="k">def</span><span class="w"> </span><span class="nf">list_users</span><span class="p">():</span>
    <span class="n">users</span> <span class="o">=</span> <span class="p">[</span>
        <span class="p">{</span><span class="s2">&quot;id&quot;</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="s2">&quot;name&quot;</span><span class="p">:</span> <span class="s2">&quot;Alice&quot;</span><span class="p">},</span>
        <span class="p">{</span><span class="s2">&quot;id&quot;</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="s2">&quot;name&quot;</span><span class="p">:</span> <span class="s2">&quot;Bob&quot;</span><span class="p">},</span>
    <span class="p">]</span>
    <span class="k">return</span> <span class="n">jsonify</span><span class="p">(</span><span class="n">users</span><span class="p">)</span>

<span class="k">if</span> <span class="vm">__name__</span> <span class="o">==</span> <span class="s2">&quot;__main__&quot;</span><span class="p">:</span>
    <span class="n">app</span><span class="o">.</span><span class="n">run</span><span class="p">(</span><span class="n">host</span><span class="o">=</span><span class="s2">&quot;0.0.0.0&quot;</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="mi">8080</span><span class="p">)</span>
</code></pre></div>

<h2 id="dockerfile">Dockerfile 示例</h2>
<div class="highlight"><pre><span></span><code><span class="k">FROM</span><span class="w"> </span><span class="s">python:3.12-slim</span>

<span class="k">WORKDIR</span><span class="w"> </span><span class="s">/app</span>
<span class="k">COPY</span><span class="w"> </span>requirements.txt<span class="w"> </span>.
<span class="k">RUN</span><span class="w"> </span>pip<span class="w"> </span>install<span class="w"> </span>--no-cache-dir<span class="w"> </span>-r<span class="w"> </span>requirements.txt

<span class="k">COPY</span><span class="w"> </span>app.py<span class="w"> </span>.
<span class="k">EXPOSE</span><span class="w"> </span><span class="s">8080</span>

<span class="k">CMD</span><span class="w"> </span><span class="p">[</span><span class="s2">&quot;python&quot;</span><span class="p">,</span><span class="w"> </span><span class="s2">&quot;app.py&quot;</span><span class="p">]</span>
</code></pre></div>

<h2 id="shell">Shell 命令</h2>
<div class="highlight"><pre><span></span><code><span class="c1"># 构建镜像</span>
docker<span class="w"> </span>build<span class="w"> </span>-t<span class="w"> </span>myapp:latest<span class="w"> </span>.

<span class="c1"># 运行容器</span>
docker<span class="w"> </span>run<span class="w"> </span>-d<span class="w"> </span>-p<span class="w"> </span><span class="m">8080</span>:8080<span class="w"> </span>--name<span class="w"> </span>myapp<span class="w"> </span>myapp:latest

<span class="c1"># 查看日志</span>
docker<span class="w"> </span>logs<span class="w"> </span>-f<span class="w"> </span>myapp

<span class="c1"># 查看运行状态</span>
curl<span class="w"> </span>http://localhost:8080/api/health
</code></pre></div>

<h2 id="yaml">YAML 配置</h2>
<p><code>docker-compose.yml</code> 示例：</p>
<div class="highlight"><pre><span></span><code><span class="nt">version</span><span class="p">:</span><span class="w"> </span><span class="s">&quot;3.8&quot;</span>
<span class="nt">services</span><span class="p">:</span>
<span class="w">  </span><span class="nt">app</span><span class="p">:</span>
<span class="w">    </span><span class="nt">build</span><span class="p">:</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">.</span>
<span class="w">    </span><span class="nt">ports</span><span class="p">:</span>
<span class="w">      </span><span class="p p-Indicator">-</span><span class="w"> </span><span class="s">&quot;8080:8080&quot;</span>
<span class="w">    </span><span class="nt">environment</span><span class="p">:</span>
<span class="w">      </span><span class="p p-Indicator">-</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">FLASK_ENV=production</span>
<span class="w">      </span><span class="p p-Indicator">-</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">LOG_LEVEL=info</span>
<span class="w">    </span><span class="nt">restart</span><span class="p">:</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">unless-stopped</span>
<span class="w">    </span><span class="nt">healthcheck</span><span class="p">:</span>
<span class="w">      </span><span class="nt">test</span><span class="p">:</span><span class="w"> </span><span class="p p-Indicator">[</span><span class="s">&quot;CMD&quot;</span><span class="p p-Indicator">,</span><span class="w"> </span><span class="s">&quot;curl&quot;</span><span class="p p-Indicator">,</span><span class="w"> </span><span class="s">&quot;-f&quot;</span><span class="p p-Indicator">,</span><span class="w"> </span><span class="s">&quot;http://localhost:8080/api/health&quot;</span><span class="p p-Indicator">]</span>
<span class="w">      </span><span class="nt">interval</span><span class="p">:</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">30s</span>
<span class="w">      </span><span class="nt">timeout</span><span class="p">:</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">5s</span>
<span class="w">      </span><span class="nt">retries</span><span class="p">:</span><span class="w"> </span><span class="l l-Scalar l-Scalar-Plain">3</span>
</code></pre></div>

<h2 id="json">JSON 配置示例</h2>
<div class="highlight"><pre><span></span><code><span class="p">{</span>
<span class="w">  </span><span class="nt">&quot;app&quot;</span><span class="p">:</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="nt">&quot;name&quot;</span><span class="p">:</span><span class="w"> </span><span class="s2">&quot;myapp&quot;</span><span class="p">,</span>
<span class="w">    </span><span class="nt">&quot;version&quot;</span><span class="p">:</span><span class="w"> </span><span class="s2">&quot;1.0.0&quot;</span><span class="p">,</span>
<span class="w">    </span><span class="nt">&quot;debug&quot;</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span>
<span class="w">  </span><span class="p">},</span>
<span class="w">  </span><span class="nt">&quot;server&quot;</span><span class="p">:</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="nt">&quot;host&quot;</span><span class="p">:</span><span class="w"> </span><span class="s2">&quot;0.0.0.0&quot;</span><span class="p">,</span>
<span class="w">    </span><span class="nt">&quot;port&quot;</span><span class="p">:</span><span class="w"> </span><span class="mi">8080</span><span class="p">,</span>
<span class="w">    </span><span class="nt">&quot;workers&quot;</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span>
<span class="w">  </span><span class="p">},</span>
<span class="w">  </span><span class="nt">&quot;database&quot;</span><span class="p">:</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="nt">&quot;host&quot;</span><span class="p">:</span><span class="w"> </span><span class="s2">&quot;db.example.com&quot;</span><span class="p">,</span>
<span class="w">    </span><span class="nt">&quot;port&quot;</span><span class="p">:</span><span class="w"> </span><span class="mi">5432</span><span class="p">,</span>
<span class="w">    </span><span class="nt">&quot;name&quot;</span><span class="p">:</span><span class="w"> </span><span class="s2">&quot;myapp_db&quot;</span>
<span class="w">  </span><span class="p">}</span>
<span class="p">}</span>
</code></pre></div>

<h2 id="_2">部署流程</h2>
<p>整个流程分为三步：</p>
<p>| 步骤 | 操作 | 预计时间 |
|------|------|----------|
| 1 | 构建 Docker 镜像 | ~2 分钟 |
| 2 | 推送到镜像仓库 | ~1 分钟 |
| 3 | 服务器拉取并运行 | ~30 秒 |</p>
<blockquote>
<p><strong>提示</strong>：生产环境建议使用 <code>docker compose</code> 管理多容器服务，配合 Nginx 反向代理和 HTTPS。</p>
</blockquote>
<h2 id="_3">小结</h2>
<p>Docker 让 Python 应用的部署变得简单可靠。通过本文的示例，你可以快速将一个 Flask 应用容器化并部署到任意支持 Docker 的服务器上。</p></content>
  </entry>
  
  <entry>
    <title>TSTL 改动</title>
    <summary>TSTL 改动</summary>
    <link href="https://691407323.github.io/posts/tstl-to-lua-about.html"/>
    <id>https://691407323.github.io/posts/tstl-to-lua-about.html</id>
    <updated>2025-07-16T00:00:00Z</updated>
    <content type="html"><p>TypeScriptToLua（简称 TSTL）是一个将 TypeScript 代码转译为 Lua 代码的工具。实际使用过程中会遇到很多需要手动处理或记住的规则，本文记录转换过程中的关键注意事项。</p>
<h2 id="vs">类方法 vs 对象方法</h2>
<p>Lua 中使用 <code>:</code> 调用时，会自动传入 <code>self</code> 作为第一个参数；使用 <code>.</code> 调用时则需要显式传参。</p>
<p>TS 中的约定与 Lua 的对应关系：</p>
<p>| TS 类型 | Lua 调用方式 |
|---------|--------------|
| 类方法（<code>static</code>） | <code>模块.xxx()</code> |
| 对象方法（非 <code>static</code>） | <code>self:xxx()</code> |</p>
<p>注意 TS 中 <code>any</code> 类型转换后也会变成 <code>::</code> 调用，因此需要对 Lua 模块文件名定义一些通用类型，例如直接定义类型、<code>typeof</code> 继承类型、<code>as unknown as xxx</code> 等方式。</p>
<h2 id="private-public-static">private / public / static</h2>
<div class="highlight"><pre><span></span><code><span class="c1">// TS 写法</span>
<span class="kd">class</span><span class="w"> </span><span class="nx">Foo</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="k">private</span><span class="w"> </span><span class="k">static</span><span class="w"> </span><span class="nx">version</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">&quot;1.0&quot;</span><span class="w">   </span><span class="c1">// → Lua: local 变量</span>
<span class="w">    </span><span class="k">public</span><span class="w"> </span><span class="k">static</span><span class="w"> </span><span class="nx">name</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">&quot;foo&quot;</span><span class="w">        </span><span class="c1">// → Lua: 模块级全局变量</span>
<span class="w">    </span><span class="k">public</span><span class="w"> </span><span class="nx">bar</span><span class="o">:</span><span class="w"> </span><span class="kt">string</span><span class="w">                </span><span class="c1">// → Lua: 对象属性</span>

<span class="w">    </span><span class="k">public</span><span class="w"> </span><span class="k">static</span><span class="w"> </span><span class="nx">init</span><span class="p">()</span><span class="w">              </span><span class="c1">// → Lua: 模块.xxx()</span>
<span class="w">    </span><span class="k">public</span><span class="w"> </span><span class="nx">update</span><span class="p">()</span><span class="w">                   </span><span class="c1">// → Lua: self:xxx()</span>
<span class="p">}</span>
</code></pre></div>

<p>对应关系：</p>
<p>| TS 修饰符 | Lua 中的表现 |
|-----------|-------------|
| <code>private</code> | Lua 模块中的 <code>local</code> |
| <code>public</code> | Lua 模块的全局 |
| <code>static</code> | 类方法，调用时用 <code>.</code> |
| 非 <code>static</code> | 对象方法，调用时用 <code>:</code> |</p>
<h2 id="this">this 参数问题</h2>
<p>需要在 TS 文件、类、方法上添加 <code>@noSelfInFile</code>、<code>@noSelf</code> 注释，或使用 <code>this: void</code> 来控制 Lua 中是否保留 <code>self</code> 参数。</p>
<div class="highlight"><pre><span></span><code><span class="cm">/** @noSelfInFile */</span><span class="w">   </span><span class="c1">// 文件级别：文件内的类不会被处理 self</span>
<span class="cm">/** @noSelf **/</span><span class="w">        </span><span class="c1">// 类或方法级别：不传递 self</span>
<span class="c1">// 或在方法签名中使用</span>
<span class="nx">method</span><span class="p">(</span><span class="nx">this</span><span class="o">:</span><span class="w"> </span><span class="kt">void</span><span class="p">)</span><span class="o">:</span><span class="w"> </span><span class="ow">void</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="p">}</span>
</code></pre></div>

<h2 id="_1">初始化与清理</h2>
<p>TS 中可提供两个特殊函数，对应 Lua 生命周期：</p>
<div class="highlight"><pre><span></span><code><span class="kd">function</span><span class="w"> </span><span class="nx">luaMInit</span><span class="p">()</span><span class="o">:</span><span class="w"> </span><span class="ow">void</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="p">}</span>
<span class="kd">function</span><span class="w"> </span><span class="nx">luaMFinal</span><span class="p">()</span><span class="o">:</span><span class="w"> </span><span class="ow">void</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="p">}</span>
</code></pre></div>

<h2 id="_2">数组索引问题</h2>
<p>TS 中的数组 <code>[]</code> 在转换到 Lua 时索引会自动 <strong>+1</strong>（Lua 索引从 1 开始）。如果不想自动 +1，改用 <code>{}</code> 代替 <code>[]</code>。</p>
<blockquote>
<p>控制这个行为的源码开关是 <code>transformElementAccessArgument</code>，如需修改可定位到 TSTL 源码。</p>
</blockquote>
<div class="highlight"><pre><span></span><code><span class="c1">// TS 写法</span>
<span class="kd">const</span><span class="w"> </span><span class="nx">arr</span><span class="o">:</span><span class="w"> </span><span class="kt">number</span><span class="p">[]</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="mf">1</span><span class="p">,</span><span class="w"> </span><span class="mf">2</span><span class="p">,</span><span class="w"> </span><span class="mf">3</span><span class="p">]</span><span class="w">   </span><span class="c1">// → Lua: {2, 3, 4}（索引 +1）</span>
<span class="kd">const</span><span class="w"> </span><span class="nx">tbl</span><span class="o">:</span><span class="w"> </span><span class="kt">LuaHashTable</span><span class="o">&lt;</span><span class="kt">number</span><span class="o">&gt;</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="p">[</span><span class="mf">0</span><span class="p">]</span><span class="o">:</span><span class="w"> </span><span class="mf">1</span><span class="p">,</span><span class="w"> </span><span class="p">[</span><span class="mf">1</span><span class="p">]</span><span class="o">:</span><span class="w"> </span><span class="mf">2</span><span class="w"> </span><span class="p">}</span><span class="w">  </span><span class="c1">// → Lua: {[0]=1, [1]=2}（不 +1）</span>
</code></pre></div>

<h2 id="_3">多参数返回</h2>
<p>使用 <code>$multi</code> 注解函数可返回多个值。使用 <code>...</code> 展开多参数时，部分场景需要配合 <code>any</code> 返回类型和去掉 <code>createUnpackCall</code> 来正确处理。</p>
<div class="highlight"><pre><span></span><code><span class="kd">function</span><span class="w"> </span><span class="nx">getPos</span><span class="p">()</span><span class="o">:</span><span class="w"> </span><span class="nx">$multi</span><span class="o">&lt;</span><span class="p">{</span><span class="w"> </span><span class="nx">x</span><span class="o">:</span><span class="w"> </span><span class="kt">number</span><span class="p">,</span><span class="w"> </span><span class="nx">y</span><span class="o">:</span><span class="w"> </span><span class="kt">number</span><span class="w"> </span><span class="p">}</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="p">...</span><span class="w"> </span><span class="p">}</span>

<span class="c1">// 调用方式</span>
<span class="kd">let</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nx">x</span><span class="p">,</span><span class="w"> </span><span class="nx">y</span><span class="w"> </span><span class="p">}</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nx">getPos</span><span class="p">()</span><span class="w">     </span><span class="c1">// 解构赋值</span>
<span class="kd">let</span><span class="w"> </span><span class="p">[</span><span class="nx">x</span><span class="p">,</span><span class="w"> </span><span class="nx">y</span><span class="p">]</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nx">getPos</span><span class="p">()</span><span class="w">       </span><span class="c1">// 数组解构</span>
<span class="kd">let</span><span class="w"> </span><span class="nx">pos</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nx">getPos</span><span class="p">()</span><span class="w">          </span><span class="c1">// 整体接收</span>
</code></pre></div>

<h2 id="tstl">TSTL 源码改动记录</h2>
<p>为解决上述问题，直接修改了 TSTL 源码。主要涉及四个文件：</p>
<h3 id="function-contextts">function-context.ts</h3>
<p><code>hasNoSelfAncestor</code> 默认返回 <code>true</code>。</p>
<h3 id="lua-astts">lua-ast.ts</h3>
<ul>
<li>注释了 <code>LuaLibFeature</code> 导入</li>
<li><code>createUnpackCall</code> 改为直接 <code>return expression</code></li>
<li>新增 <code>wrapInTable</code> 逻辑</li>
</ul>
<h3 id="accessts">access.ts</h3>
<p>数组索引偏移从 <code>+1</code> 改为 <code>+0</code>，使 TS 数组索引与 Lua 保持一致。</p>
<h3 id="callts">call.ts</h3>
<ul>
<li>新增 <code>static</code> 方法判断逻辑</li>
<li><code>wrapResultInTable</code> 强制为 <code>false</code></li>
</ul>
<h3 id="functionts">function.ts</h3>
<p><code>static</code> 方法不绑定 <code>self</code> 参数。</p>
<h2 id="_4">小结</h2>
<p>本文记录 TypeScriptToLua 转换中最常遇到的核心规则，包括方法调用方式、类型可见性、this 处理、数组索引偏移、多返回值等，以及对应的源码修改。掌握这些规则能大幅减少转译后的调试成本。</p></content>
  </entry>
  
  <entry>
    <title>TSTL 插件架构</title>
    <summary>TSTL 插件架构</summary>
    <link href="https://691407323.github.io/posts/tstl-plugin-architecture.html"/>
    <id>https://691407323.github.io/posts/tstl-plugin-architecture.html</id>
    <updated>2025-03-16T00:00:00Z</updated>
    <content type="html"><p>本文系统梳理一套基于 TypeScriptToLua (TSTL) 的自定义插件的完整实现，涵盖从增量编译到 AST 转换、代码生成的全流程。</p>
<h2 id="_1">整体架构</h2>
<p>这套插件的核心目标：</p>
<ul>
<li>将 TypeScript 编译为接近手写 Lua 5.3/5.4 的代码</li>
<li>移除模块系统（不生成 require/exports），类作为全局模块</li>
<li>简化类模型（用 <code>____M</code> 变量代替复杂 <code>__TS__Class()</code> 包装）</li>
<li>内联特殊方法（luaMInit / luaMFinal / init 静态方法直接展开）</li>
<li>支持增量编译 + 内嵌原生 Lua 代码</li>
</ul>
<p>插件执行顺序：<code>beforeTransform</code> → <code>visitors</code> → <code>beforeEmit</code> → <code>afterPrint</code> → <code>printer</code> → <code>afterEmit</code></p>
<h2 id="beforetransformts">beforeTransform.ts — 增量编译</h2>
<p>实现增量编译 + 自动复制非 TS 资源文件到输出目录。</p>
<p>通过 <code>file-timestamps.txt</code> 记录每个源文件的 ctime+mtime 组合，判断文件是否有变化。未变化的文件从 <code>program.getSourceFiles()</code> 中移除，TSTL 直接跳过。同时递归复制非 <code>.ts</code> 资源文件到输出目录，同样带时间戳校验。</p>
<div class="highlight"><pre><span></span><code><span class="kd">class</span><span class="w"> </span><span class="nx">Plugin</span><span class="w"> </span><span class="k">implements</span><span class="w"> </span><span class="nx">tstl</span><span class="p">.</span><span class="nx">Plugin</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="k">private</span><span class="w"> </span><span class="nx">unchangedFiles</span><span class="o">:</span><span class="w"> </span><span class="kt">ts.SourceFile</span><span class="p">[]</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">[];</span>
<span class="w">    </span><span class="k">private</span><span class="w"> </span><span class="nx">timestamps</span><span class="o">:</span><span class="w"> </span><span class="kt">Record</span><span class="o">&lt;</span><span class="kt">string</span><span class="p">,</span><span class="w"> </span><span class="kt">string</span><span class="o">&gt;</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">{};</span>
<span class="w">    </span><span class="k">private</span><span class="w"> </span><span class="nx">timestampFile</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nx">path</span><span class="p">.</span><span class="nx">resolve</span><span class="p">(</span><span class="s2">&quot;./&quot;</span><span class="p">,</span><span class="w"> </span><span class="s2">&quot;file-timestamps.txt&quot;</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div>

<p>关键逻辑：</p>
<ol>
<li>加载历史时间戳</li>
<li>检查每个源文件的 <code>ctime+mtime</code> 组合字符串，判断是否有变化</li>
<li>未变化的文件从 <code>program.getSourceFiles()</code> 中 splice 移除</li>
<li>递归复制非 <code>.ts</code> 资源文件到输出目录</li>
<li>将更新后的时间戳写回文件</li>
</ol>
<h2 id="visitorsts-ast">visitors.ts — 核心 AST 转换（最重要）</h2>
<p>注册了多个自定义 Visitor，重写默认的 TS→Lua 转换逻辑。</p>
<h3 id="_2">注册入口</h3>
<div class="highlight"><pre><span></span><code><span class="kd">const</span><span class="w"> </span><span class="nx">plugin</span><span class="o">:</span><span class="w"> </span><span class="kt">tstl.Plugin</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="nx">visitors</span><span class="o">:</span><span class="w"> </span><span class="p">{</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">ImportDeclaration</span><span class="p">]</span><span class="o">:</span><span class="w">  </span><span class="p">(</span><span class="nx">node</span><span class="p">,</span><span class="w"> </span><span class="nx">context</span><span class="p">)</span><span class="w"> </span><span class="p">=&gt;</span><span class="w"> </span><span class="kc">undefined</span><span class="p">,</span><span class="w">     </span><span class="c1">// 移除 import</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">ExportDeclaration</span><span class="p">]</span><span class="o">:</span><span class="w">  </span><span class="p">(</span><span class="nx">node</span><span class="p">,</span><span class="w"> </span><span class="nx">context</span><span class="p">)</span><span class="w"> </span><span class="p">=&gt;</span><span class="w"> </span><span class="kc">undefined</span><span class="p">,</span><span class="w">     </span><span class="c1">// 移除 export</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">ClassDeclaration</span><span class="p">]</span><span class="o">:</span><span class="w">   </span><span class="nx">visitClassDeclaration2</span><span class="p">,</span><span class="w">            </span><span class="c1">// 核心：类重写</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">ThisKeyword</span><span class="p">]</span><span class="o">:</span><span class="w">        </span><span class="nx">visitThis</span><span class="p">,</span><span class="w">                         </span><span class="c1">// this 指针处理</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">SourceFile</span><span class="p">]</span><span class="o">:</span><span class="w">         </span><span class="nx">sourceFile</span><span class="p">,</span><span class="w">                        </span><span class="c1">// 重置计数器</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">CallExpression</span><span class="p">]</span><span class="o">:</span><span class="w">     </span><span class="nx">callExpression</span><span class="p">,</span><span class="w">                    </span><span class="c1">// 方法调用</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">PropertyAccessExpression</span><span class="p">]</span><span class="o">:</span><span class="w"> </span><span class="nx">propertyAccessExpression</span><span class="p">,</span><span class="w">    </span><span class="c1">// 属性访问</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">Identifier</span><span class="p">]</span><span class="o">:</span><span class="w">         </span><span class="nx">identifier</span><span class="p">,</span><span class="w">                        </span><span class="c1">// 特殊标识符</span>
<span class="w">        </span><span class="p">[</span><span class="nx">ts</span><span class="p">.</span><span class="nx">SyntaxKind</span><span class="p">.</span><span class="nx">FunctionDeclaration</span><span class="p">]</span><span class="o">:</span><span class="w"> </span><span class="nx">functionDeclaration</span><span class="p">,</span><span class="w">              </span><span class="c1">// 函数特殊处理</span>
<span class="w">    </span><span class="p">},</span>
<span class="p">};</span>
</code></pre></div>

<h3 id="visitclassdeclaration2">类声明核心转换（visitClassDeclaration2）</h3>
<p>用 <code>____M</code>（或其变体）代替 <code>__TS__Class()</code>，简化类模型，处理 init / private / static。</p>
<p>核心产出（Lua）：</p>
<div class="highlight"><pre><span></span><code><span class="kd">local</span><span class="w"> </span><span class="nv">____M</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">__TS__Class</span><span class="p">()</span><span class="w">          </span><span class="c1">-- 基础类壳</span>
<span class="nv">MyClass</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nv">____M</span><span class="w">                      </span><span class="c1">-- 全局注入</span>
<span class="kd">local</span><span class="w"> </span><span class="nv">____M</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nv">MyClass</span><span class="w">                </span><span class="c1">-- 模块变量声明</span>
<span class="c1">-- ...成员绑定...</span>
</code></pre></div>

<p>关键概念：</p>
<p>| 概念 | 说明 |
|------|------|
| <code>____M</code> | 类模块变量，代替默认 <code>__TS__Class()</code> 的类引用 |
| <code>____M_tempN</code> | 嵌套类时的模块变量，N 为嵌套层级 |
| <code>____ThisBind_temp</code> | 静态方法中访问 private 成员时的 this 代理变量 |
| <code>____LocalFuncBind_temp</code> | private 方法绑定的表对象 |
| <code>self</code> | 实例方法中的 this，对应 Lua 的 self |</p>
<h3 id="this-visitthis">this 关键字处理（visitThis）</h3>
<p>转换规则：</p>
<ul>
<li>实例方法中 <code>this</code> → <code>self</code></li>
<li>静态方法中 <code>this</code> → <code>____M</code>（或嵌套时的 <code>____M_tempN</code>）</li>
<li>静态方法中访问 private 成员 → <code>____ThisBind_temp</code></li>
</ul>
<h3 id="callexpression">方法调用处理（callExpression）</h3>
<p>核心区分逻辑：</p>
<ol>
<li><code>super.xxx()</code> → 原型方法调用（<code>super.____constructor(...)</code> 等）</li>
<li><code>private static</code> 方法 → 直接调用函数名：<code>methodName(args)</code></li>
<li><code>public static</code> 方法 → 带模块名调用</li>
<li>普通实例方法 → 走默认转换</li>
</ol>
<h3 id="propertyaccessexpression">属性访问处理（propertyAccessExpression）</h3>
<ul>
<li><code>private static</code> 属性/方法访问 → 直接用属性名（不加模块前缀）</li>
<li>其他情况走默认转换</li>
</ul>
<h3 id="identifier">特殊标识符（identifier）</h3>
<p>核心：<code>__FUNC__</code> 魔术常量</p>
<div class="highlight"><pre><span></span><code><span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="nx">node</span><span class="p">.</span><span class="nx">text</span><span class="w"> </span><span class="o">===</span><span class="w"> </span><span class="s2">&quot;__FUNC__&quot;</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="c1">// 自动替换为当前函数名（&quot;类名.方法名&quot; 格式）</span>
<span class="w">    </span><span class="nx">functionName</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="sb">`</span><span class="si">${</span><span class="nx">className</span><span class="si">}</span><span class="sb">.</span><span class="si">${</span><span class="nx">methodName</span><span class="si">}</span><span class="sb">`</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div>

<h3 id="functiondeclaration">函数声明处理（functionDeclaration）</h3>
<p>对名为 <code>luaMInit</code> 的顶级函数，直接展开为语句块（不生成函数定义）。其余函数走默认转换。</p>
<h2 id="beforeemitts">beforeEmit.ts — 生成前深度处理</h2>
<p>在 Lua 文件写入磁盘前做最后的字符串和 AST 处理。</p>
<h3 id="_3">特殊函数替换</h3>
<p>| TS 宏 | 替换结果 |
|-------|---------|
| <code>TSRequire(x)</code> | <code>require('x')</code> |
| <code>StrRequire(x)</code> | <code>require(x)</code> |
| <code>LuaCode("...")</code> | 直接嵌入原始 Lua 字符串 |
| <code>LuaMultiReturnFunc(...)</code> | 展开内部内容 |
| <code>Local(xyz)</code> | <code>local xyz = xyz</code> |</p>
<h3 id="ast">AST 裁剪</h3>
<p>用 <code>luaparse</code> 解析 Lua AST，定位 <code>luaMInitFunc = function(...)</code> 赋值语句，只保留函数体内部内容，剥离外层变量赋值包裹。</p>
<h3 id="return">类模块追加 return</h3>
<p>通过匹配 <code>Xxx.name = "..."</code> 提取类名，文件末尾追加 <code>return ____M</code>。</p>
<div class="highlight"><pre><span></span><code><span class="kd">let</span><span class="w"> </span><span class="nx">match</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nx">emitFile</span><span class="p">.</span><span class="nx">code</span><span class="p">.</span><span class="nx">match</span><span class="p">(</span><span class="sr">/(\w+)\.name\s*=\s*&quot;/</span><span class="p">);</span>
<span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="nx">match</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="nx">emitFile</span><span class="p">.</span><span class="nx">code</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nx">emitFile</span><span class="p">.</span><span class="nx">code</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="s1">&#39;\nreturn ____M\n&#39;</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div>

<h2 id="afterprintts">afterPrint.ts — 打印后文本处理</h2>
<p>对已格式化的 Lua 代码做字符串层面的后处理。</p>
<ul>
<li>移除 <code>____exports</code> 相关行</li>
<li>移除 <code>____ThisBind_temp.</code> 前缀</li>
<li>移除 <code>____LocalFuncBind_temp.</code> 前缀</li>
</ul>
<h2 id="printerts-lua">printer.ts — 自定义 Lua 打印机</h2>
<p>继承 <code>LuaPrinter</code>，重写 <code>printFile</code> 在文件头添加注释。</p>
<div class="highlight"><pre><span></span><code><span class="kd">class</span><span class="w"> </span><span class="nx">CustomPrinter</span><span class="w"> </span><span class="k">extends</span><span class="w"> </span><span class="nx">tstl</span><span class="p">.</span><span class="nx">LuaPrinter</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="k">protected</span><span class="w"> </span><span class="nx">printFile</span><span class="p">(</span><span class="nx">file</span><span class="o">:</span><span class="w"> </span><span class="kt">tstl.File</span><span class="p">)</span><span class="o">:</span><span class="w"> </span><span class="nx">SourceNode</span><span class="w"> </span><span class="p">{</span>
<span class="w">        </span><span class="kd">const</span><span class="w"> </span><span class="nx">originalResult</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="k">super</span><span class="p">.</span><span class="nx">printFile</span><span class="p">(</span><span class="nx">file</span><span class="p">);</span>
<span class="w">        </span><span class="k">return</span><span class="w"> </span><span class="k">this</span><span class="p">.</span><span class="nx">createSourceNode</span><span class="p">(</span><span class="nx">file</span><span class="p">,</span><span class="w"> </span><span class="p">[</span>
<span class="w">            </span><span class="sb">`</span><span class="si">${</span><span class="nx">CUSTOM_COMMENT_HEADER</span><span class="si">}</span><span class="sb"> </span><span class="si">${</span><span class="k">this</span><span class="p">.</span><span class="nx">luaFile</span><span class="si">}</span><span class="sb">\n`</span><span class="p">,</span>
<span class="w">            </span><span class="nx">originalResult</span>
<span class="w">        </span><span class="p">]);</span>
<span class="w">    </span><span class="p">}</span>
<span class="p">}</span>
</code></pre></div>

<h2 id="afteremitts">afterEmit.ts — 生成后回调</h2>
<p>在 Lua 代码生成完成后触发，记录输出文件信息，打印 TS 原始文件路径和输出文件路径。</p>
<h2 id="moduleresolutionts">模块解析（moduleResolution.ts）</h2>
<p>简单的模块重定向示例，将特定模块名映射到不同文件。</p>
<div class="highlight"><pre><span></span><code><span class="kr">module</span><span class="nx">Resolution</span><span class="p">(</span><span class="kr">module</span><span class="nx">Identifier</span><span class="o">:</span><span class="w"> </span><span class="kt">string</span><span class="p">,</span><span class="w"> </span><span class="p">...)</span><span class="w"> </span><span class="p">{</span>
<span class="w">    </span><span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="kr">module</span><span class="nx">Identifier</span><span class="w"> </span><span class="o">===</span><span class="w"> </span><span class="s2">&quot;foo&quot;</span><span class="p">)</span><span class="w"> </span><span class="p">{</span>
<span class="w">        </span><span class="k">return</span><span class="w"> </span><span class="s2">&quot;bar&quot;</span><span class="p">;</span>
<span class="w">    </span><span class="p">}</span>
<span class="p">}</span>
</code></pre></div>

<h2 id="_4">编译流水线总结</h2>
<div class="highlight"><pre><span></span><code><span class="err">┌─────────────────────────────────────────────────────────────────┐</span>
<span class="err">│</span><span class="w">                        </span><span class="n">TSTL</span><span class="w"> </span><span class="err">编译流水线</span><span class="w">                           </span><span class="err">│</span>
<span class="err">├─────────────────────────────────────────────────────────────────┤</span>
<span class="err">│</span><span class="w">  </span><span class="n">Phase</span><span class="w"> </span><span class="mi">1</span><span class="p">:</span><span class="w"> </span><span class="err">元数据准备</span><span class="w">                                              </span><span class="err">│</span>
<span class="err">│</span><span class="w">    </span><span class="err">→</span><span class="w"> </span><span class="n">beforeTransform</span><span class="o">.</span><span class="n">ts</span><span class="p">:</span><span class="w"> </span><span class="err">增量编译，剔除未修改文件，复制资源文件</span><span class="w">       </span><span class="err">│</span>
<span class="err">│</span><span class="w">                                                                  </span><span class="err">│</span>
<span class="err">│</span><span class="w">  </span><span class="n">Phase</span><span class="w"> </span><span class="mi">2</span><span class="p">:</span><span class="w"> </span><span class="n">AST</span><span class="w"> </span><span class="err">转换（</span><span class="n">Visitors</span><span class="err">）</span><span class="w">                                   </span><span class="err">│</span>
<span class="err">│</span><span class="w">    </span><span class="err">→</span><span class="w"> </span><span class="n">visitors</span><span class="o">.</span><span class="n">ts</span><span class="w"> </span><span class="err">介入，重写</span><span class="w"> </span><span class="n">Class</span><span class="o">/</span><span class="n">This</span><span class="o">/</span><span class="n">Call</span><span class="o">/</span><span class="n">Identifier</span><span class="w"> </span><span class="err">等</span><span class="w">         </span><span class="err">│</span>
<span class="err">│</span><span class="w">       </span><span class="err">核心产出：简化的</span><span class="w"> </span><span class="n">Lua</span><span class="w"> </span><span class="n">AST</span><span class="err">（使用</span><span class="w"> </span><span class="n">____M</span><span class="p">,</span><span class="w"> </span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">local</span><span class="w"> </span><span class="err">等）</span><span class="w">        </span><span class="err">│</span>
<span class="err">│</span><span class="w">                                                                  </span><span class="err">│</span>
<span class="err">│</span><span class="w">  </span><span class="n">Phase</span><span class="w"> </span><span class="mi">3</span><span class="p">:</span><span class="w"> </span><span class="n">Lua</span><span class="w"> </span><span class="err">代码打印</span><span class="w">                                            </span><span class="err">│</span>
<span class="err">│</span><span class="w">    </span><span class="err">→</span><span class="w"> </span><span class="n">printer</span><span class="o">.</span><span class="n">ts</span><span class="p">:</span><span class="w"> </span><span class="err">可选自定义打印（注释头）</span><span class="w">                          </span><span class="err">│</span>
<span class="err">│</span><span class="w">    </span><span class="err">→</span><span class="w"> </span><span class="err">默认</span><span class="w"> </span><span class="n">TSTL</span><span class="w"> </span><span class="n">LuaPrinter</span><span class="w"> </span><span class="err">输出</span><span class="w"> </span><span class="n">Lua</span><span class="w"> </span><span class="err">字符串</span><span class="w">                          </span><span class="err">│</span>
<span class="err">│</span><span class="w">                                                                  </span><span class="err">│</span>
<span class="err">│</span><span class="w">  </span><span class="n">Phase</span><span class="w"> </span><span class="mi">4</span><span class="p">:</span><span class="w"> </span><span class="err">打印后处理</span><span class="w">                                              </span><span class="err">│</span>
<span class="err">│</span><span class="w">    </span><span class="err">→</span><span class="w"> </span><span class="n">afterPrint</span><span class="o">.</span><span class="n">ts</span><span class="p">:</span><span class="w"> </span><span class="err">字符串替换，清理</span><span class="w"> </span><span class="n">____exports</span><span class="o">/</span><span class="n">____ThisBind_temp</span><span class="w">  </span><span class="err">│</span>
<span class="err">│</span><span class="w">                                                                  </span><span class="err">│</span>
<span class="err">│</span><span class="w">  </span><span class="n">Phase</span><span class="w"> </span><span class="mi">5</span><span class="p">:</span><span class="w"> </span><span class="err">生成前处理</span><span class="w">                                              </span><span class="err">│</span>
<span class="err">│</span><span class="w">    </span><span class="err">→</span><span class="w"> </span><span class="n">beforeEmit</span><span class="o">.</span><span class="n">ts</span><span class="p">:</span><span class="w"> </span><span class="n">TSRequire</span><span class="err">→</span><span class="n">require</span><span class="p">,</span><span class="w"> </span><span class="n">LuaCode嵌入</span><span class="p">,</span><span class="w"> </span><span class="n">AST裁剪</span><span class="p">,</span><span class="w">       </span><span class="err">│</span>
<span class="err">│</span><span class="w">       </span><span class="n">Local宏展开</span><span class="p">,</span><span class="w"> </span><span class="err">格式化</span><span class="p">,</span><span class="w"> </span><span class="err">追加</span><span class="w"> </span><span class="k">return</span><span class="w"> </span><span class="n">____M</span><span class="w">                       </span><span class="err">│</span>
<span class="err">│</span><span class="w">                                                                  </span><span class="err">│</span>
<span class="err">│</span><span class="w">  </span><span class="n">Phase</span><span class="w"> </span><span class="mi">6</span><span class="p">:</span><span class="w"> </span><span class="err">文件写入磁盘</span><span class="w">                                            </span><span class="err">│</span>
<span class="err">│</span><span class="w">                                                                  </span><span class="err">│</span>
<span class="err">│</span><span class="w">  </span><span class="n">Phase</span><span class="w"> </span><span class="mi">7</span><span class="p">:</span><span class="w"> </span><span class="err">生成后回调</span><span class="w">                                              </span><span class="err">│</span>
<span class="err">│</span><span class="w">    </span><span class="err">→</span><span class="w"> </span><span class="n">afterEmit</span><span class="o">.</span><span class="n">ts</span><span class="p">:</span><span class="w"> </span><span class="err">记录日志，打印原始文件和输出文件路径</span><span class="w">              </span><span class="err">│</span>
<span class="err">└─────────────────────────────────────────────────────────────────┘</span>
</code></pre></div></content>
  </entry>
  
</feed>